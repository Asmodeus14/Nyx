[package]
name = "nyx-terminal"
version = "0.1.0"
edition = "2021"

[dependencies]

linked_list_allocator = "0.10.5"
nyx-api = { path = "../../libs/api" }
nyx-gui = { path = "../../libs/gui" }

[profile.release]
panic = "abort"
opt-level = 3