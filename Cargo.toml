[workspace]
members = [
    "nyx-kernel",
    "compiler",
    "runner",
    "nyx-user"
, "nyx-rust-app"]

# NEW: Force Cargo to ONLY build the kernel by default
default-members = ["nyx-kernel"]

resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
